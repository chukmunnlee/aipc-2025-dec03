all:
  vars:
    ansible_user: ${cs_user}
    ansible_ssh_private_key_file: ${ssh_private_key_file}
    ansible_connection: ssh
    codeserver_password: ${cs_password}

  hosts:
    codeserver:
      ansible_host: ${droplet_ip}
